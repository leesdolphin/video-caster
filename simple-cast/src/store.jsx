
export const SAVE_CACHE = 'save-cache'
export const SAVED_CACHE = 'saved-cache'

export function cachingMiddleware (saving) {
  return ({dispatch, getState}) => {
    return (next) => (action) => {

    }
  }
}
